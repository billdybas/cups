{
  "name": "cups",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "build": "backpack build",
    "start": "node ./build/main.js",
    "watch": "backpack",
    "postinstall": "yarn run build"
  },
  "license": "MIT",
  "dependencies": {
    "backpack-core": "^0.8.2",
    "express": "^4.16.4"
  }
}
